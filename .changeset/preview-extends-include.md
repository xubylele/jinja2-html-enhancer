---
"jinja2-html-enhancer": patch
---

Template preview now renders templates that use `{% extends %}` and `{% include %}`. Previously, the preview showed raw Jinja2 tags instead of the rendered output.
