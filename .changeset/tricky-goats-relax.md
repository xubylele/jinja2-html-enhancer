---
"jinja2-html-enhancer": minor
---

Add 10 built-in Jinja2 code snippets. Type `for`, `if`, `ife`, `block`, `extends`, `include`, `set`, `macro`, `with`, or `filter` in any `.html` file and get a ready-to-use Jinja2 snippet with tabstop placeholders.
